export enum ViewIds {
  BaseFlow = 'BaseFlow',
}

export enum PanelIds {
  Home = 'Home',
  MyTests = 'MyTests',
  Test = 'Test',
  Success = 'Success',
  Results = 'Results',
}

export enum ModalIds {
  TestModal = 'TestModal',
}
